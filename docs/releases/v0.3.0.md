# Sidekick v0.3.0 — Stability & Contract Release

**Tag:** `v0.3.0`
**Datum:** 2026-06-07
**Basierend auf:** `v0.2.0` (`5f9edfe`)

---

## Ziel des Releases

Stabilisierung des Sidekick-Produktkerns: Session-Contract zwischen
CLI/TUI/WebUI vereinheitlichen, Gateway-Warnings beseitigen, CI/Smoke
erweitern, Config/Env-Alias-Fallback-Chain absichern.

Keine neuen Features. Keine LastBrowser-Arbeit.

---

## Änderungen

### 1. Session Contract härten (Commit `1905fca`)

**Analyse:** `shared/sessions.py` (CLI/TUI) und `web.api.models.Session`
(WebUI) hatten unterschiedliche Storage-Pfade — Sessions aus der CLI waren
im WebUI unsichtbar und umgekehrt.

**Storage-Pfad-Vereinheitlichung:**

| Quelle | Alt | Neu |
|--------|-----|-----|
| `web/api/config.py` `STATE_DIR` | `HERMES_WEBUI_STATE_DIR` > `~/.hermes/webui/` | `SIDEKICK_WEBUI_STATE_DIR` > `HERMES_WEBUI_STATE_DIR` > `SIDEKICK_STATE_DIR` > `HERMES_STATE_DIR` > `~/.sidekick/state/` |
| `shared.runtime.web_state_dir()` | `SIDEKICK_WEBUI_STATE_DIR` > `HERMES_WEBUI_STATE_DIR` > `~/.sidekick/state/webui/` | unverändert (deckt jetzt denselben Pfad) |

**Legacy-Session-Migration** (`shared/sessions.migrate_legacy_sessions()`):
Einmalige Kopie alter `~/.hermes/webui/sessions/*.json` in den kanonischen
Pfad. Automatisch getriggert beim ersten `list_sessions()`-Aufruf.

**Thin Wrapper:** `web/api/session_ops.py` delegiert `retry_last`,
`undo_last`, `session_status` an `shared.sessions` und fügt nur WebUI-
spezifische Locks und Stream-Checks hinzu.

### 2. Gateway-Warnings fixen (Commit `2d243be`)

2 non-blocking ImportError-Warnings beim Gateway-Start:

```
Warning: config validation failed: cannot import name 'print_config_warnings'
Warning: deprecation check failed: cannot import name 'warn_deprecated_cwd_env_vars'
```

**Ursache:** `sidekick_cli.config` routet via Shim zu `runtime.config`, dort
fehlten die beiden Startup-Funktionen.

**Fix:** Stub-Implementierungen in `runtime/config.py` — No-Ops mit Doku.
Im Monorepo deckt `sidekick doctor` die Config-Strukturprüfung ab.

**Ergebnis:** Gateway importiert sauber, 0 Warnings.

### 3. CI/Smoke erweitert (Commit kommt mit diesem Release)

| Test | v0.2.0 | v0.3.0 |
|------|--------|--------|
| Core bootstrap | 4 | 4 |
| Import smoke | 3 | 5 |
| Config/Env/Paths | 0 | 2 |
| Session layer | 1 | 4 |
| TUI smoke | 0 | 1 |
| **Gesamt** | **10** | **16** |

Neue Tests:
- `gateway.run import` (0 Warnings)
- `shared.paths: sidekick_home resolution`
- `shared.paths: env var priority (SIDEKICK > HERMES)`
- `shared.sessions: create/append/retry/undo` (4-msg convo)
- `web.api.session_ops: delegation`
- `shared.sessions: legacy migration mock`
- `shared.sessions: list_sessions`
- `TUI module import (curses_ui)`

### 4. Config/Env-Alias-Tests

Env-Var-Fallback-Chain getestet:

| Test | Ergebnis |
|------|----------|
| `SIDEKICK_HOME` hat Vorrang vor `HERMES_HOME` | ✅ |
| `HERMES_HOME` wird als Fallback gelesen | ✅ |
| Default auf `~/.sidekick` | ✅ |
| CI deckt beide Alias-Pfade ab | ✅ |

---

## Smoke-Test-Ergebnis

```text
✓ pip install -e .
✓ sidekick --help
✓ sidekick --version
✓ sidekick doctor
✓ CLI import (cli.cli)
✓ run_agent import (AIAgent)
✓ tools registry
✓ web.server import
✓ gateway.run import (0 warnings)
✓ shared.paths: sidekick_home resolution
✓ shared.paths: env var priority (SIDEKICK > HERMES)
✓ shared.sessions: create/append/retry/undo
✓ web.api.session_ops: delegation
✓ shared.sessions: legacy migration mock
✓ shared.sessions: list_sessions
✓ TUI module import (curses_ui)
=== Ergebnis: 16 passed, 0 failed ===
```

---

## Tag-/Commit-Referenzen

| Commit | Hash | Beschreibung |
|--------|------|-------------|
| Session Contract | `1905fca` | Storage-Pfad vereinheitlicht + thin wrappers + legacy migration |
| Gateway-Warnings | `2d243be` | Stubs für print_config_warnings/warn_deprecated_cwd_env_vars |
| Smoke + Tests | *dieser Commit* | 16 Tests, Config/Env-Alias-CI |
| **Release** | **`v0.3.0`** | |

---

## Offene Risiken (v0.4.0 oder später)

- **Session-Layer:** `web.api.models.Session` (30+ Felder) vs
  `shared.sessions.Session` (6 Felder) sind noch unterschiedliche Klassen.
  Die Persistenz ist vereinheitlicht, die Objektmodelle bleiben getrennt —
  das WebUI braucht agent state, streaming, compression anchor etc.
- **Gateway:** Der Bug `await outside async function` wurde in `v0.1.0`
  behoben. Keine weiteren bekannten Gateway-Probleme.
- **CI:** Läuft auf ubuntu-latest. Windows- und macOS-Tests sind für später.

---

## Ausblick v0.4.0

- Keine großen Features
- Gateway-Plattform-Tests
- Windows-CI-Matrix hinzufügen
- `sidekick_cli/`-Forwarder aufräumen (nach erfolgreichem QA der Shims)
