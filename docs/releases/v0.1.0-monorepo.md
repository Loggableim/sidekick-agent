# Sidekick v0.1.0-monorepo — Release Notes

**Tag:** `v0.1.0-monorepo`
**Datum:** 2026-06-07
**Commit:** `ddd8208`
**Remote:** `https://github.com/Loggableim/sidekick-agent`

---

## Ziel des Releases

Erster veröffentlichter Stand des Sidekick-Monorepos. Ersetzt die historische
Trennung zwischen `cids-hermes-agent` (Agent-Runtime + CLI + Tools) und
`cids-hermes-webui` (WebUI-Server + Frontend) durch ein einziges installierbares
Paket mit drei First-Class-Surfaces: **CLI**, **TUI** und **WebUI**.

---

## Wichtigste Strukturänderungen

| Alt (getrennt) | Neu (monorepo) |
|----------------|----------------|
| `cids-hermes-agent/agent/*.py` | `runtime/*.py` |
| `cids-hermes-agent/cron/` | `runtime/cron/` |
| `cids-hermes-agent/gateway/` | `runtime/gateway/` |
| `cids-hermes-agent/cli.py + sidekick_cli/` | `cli/` (77 Module) |
| `cids-hermes-agent/run_agent.py` | `./run_agent.py` |
| `cids-hermes-agent/tools/` | `tools/` (~100 Module) |
| `cids-hermes-webui/api/` | `web/api/` (48 Module) |
| `cids-hermes-webui/static/` | `web/static/` (113 Dateien) |

**Neue/vereinheitlichte Layer:**

| Layer | Beschreibung |
|-------|-------------|
| `shared/` | Config, Paths, Sessions, Logging, Utils — von beiden Surfaces genutzt |
| `sidekick_app/` | Entrypoint mit Legacy-Import-Bootstrap |
| `sidekick_cli/` | Transition-Layer (leitet `from sidekick_cli.*` an `cli.*` weiter) |
| `runtime/_compat/` | Shim-Module für alte Import-Pfade |

---

## Neue Entry Points

| Command | Beschreibung |
|---------|-------------|
| `sidekick --help` | Vollständige CLI-Referenz (38+ Subcommands) |
| `sidekick` | Interaktiver Chat mit AIAgent (REPL) |
| `sidekick --tui` | Terminal-UI-Modus (prompt_toolkit) |
| `sidekick doctor` | System Health Check |
| `sidekick dashboard` | WebUI-Server starten (http://127.0.0.1:8787) |
| `sidekick setup` | Interactive Setup Wizard |
| `sidekick status` | Komponenten-Status |
| `sidekick login` | Provider-Authentifizierung |

Legacy `hermes`-Alias:

```bash
hermes --help    # gleicher Entrypoint
```

---

## Legacy-Kompatibilität

### Umgebungsvariablen

| Alt | Neu | Status |
|-----|-----|--------|
| `HERMES_HOME` | `SIDEKICK_HOME` | ✅ Beide lesbar |
| `HERMES_STATE_DIR` | `SIDEKICK_STATE_DIR` | ✅ Beide lesbar |
| `HERMES_WEBUI_HOST/PORT` | `SIDEKICK_WEBUI_HOST/PORT` | ✅ Beide lesbar |
| `HERMES_OPTIONAL_SKILLS` | `SIDEKICK_OPTIONAL_SKILLS` | ✅ Beide lesbar |

### Home-Verzeichnis
1. `$SIDEKICK_HOME` → `~/.sidekick/` (kanonisch)
2. `$HERMES_HOME` → `~/.hermes/` (Legacy-Fallback)

### CLI
- `hermes` → `sidekick` (Alias in pyproject.toml)

### Python-Imports
- `from sidekick_cli.*` → leitet an `cli.*` weiter
- `from hermes_constants` → re-exportiert `runtime._compat.shim_constants`
- `from sidekick_state` → re-exportiert `runtime._compat.shim_state`
- `from agent.*` → **nicht mehr vorhanden** (alle `from agent.` => `from runtime.`)

---

## Bekannte Einschränkungen

### Non-blocking Warnings

```text
Warning: config validation failed: cannot import name 'print_config_warnings'
         from 'runtime.config'
Warning: deprecation check failed: cannot import name 'warn_deprecated_cwd_env_vars'
         from 'runtime.config'
```

Diese 2 Meldungen erscheinen beim Start von `runtime.gateway.run` und sind
harmlos. Die Funktionen existierten im alten `sidekick_cli.config` und werden
beim nächsten Gateway-Refactor portiert oder entfernt.

### Gateway await-Bug (gefixt)

Der Bug `await outside async function` in `runtime/gateway/run.py` war ein
pre-existing Bug im Original-Source und wurde in diesem Release isoliert
behoben (`asyncio.run_coroutine_threadsafe` für Pool-Acquire/Release in
Sync-Callback).

### Session-Vertrag

Zwei Session-Modelle koexistieren:
- `shared/sessions.py` — kanonisches Modell mit `retry_last`, `undo_last`,
  `session_status` (für CLI/TUI)
- `web/api/session_ops.py` — WebUI-spezifische Wrapper mit Agent-Lock
  und Stream-Lifecycle (delegiert generische Ops an `shared.sessions`)

Beide teilen sich denselben Storage-Pfad (`~/.sidekick/state/webui/sessions/`).
Zusammenführung in `v0.2.0` geplant.

### CLI-Help-Strings

Die Subcommand-Beschreibungen in `sidekick --help` erwähnen noch teilweise
"Hermes". Diese sind kosmetisch und werden in `v0.2.0` bereinigt.

### WebUI localStorage-Keys

Die WebUI verwendet noch `hermes-theme`, `hermes-skin` etc. als
localStorage-Keys. Funktionieren einwandfrei, aber Kosmetik für `v0.2.0`.

---

## Smoke-Test-Ergebnis

```text
✓ pip install
✓ sidekick --help
✓ sidekick --version
✓ sidekick doctor
✓ CLI import (cli.cli)
✓ run_agent import (AIAgent)
✓ tools registry (76 tools)
✓ web.server import
✓ shared sessions (create/read/retry/undo)
✓ config bootstrap
=== Ergebnis: 10 passed, 0 failed ===
```

---

## Tag-/Commit-Referenzen

| Commit | Hash | Beschreibung |
|--------|------|--------------|
| Baseline | `553cc9c` | Bootstrap standalone sidekick monorepo baseline |
| Phase 1+2 | `d77b2ff` | Migrate runtime core, cron, and gateway |
| Phase 3 | `c80b258` | Migrate CLI/TUI |
| Phase 4 | `f93230a` | Migrate WebUI |
| Bootability | `4883983` | CLI entrypoint, tools stubs, run_agent shim |
| Bootability 2 | `d1f3a07` | WebUI imports, cron shims |
| Phase 6 | `9ad7c7c` | Rebranding (Hermes/Nous entfernt) |
| Tools+run_agent | `2702426` | Port tools/ and run_agent.py |
| Bootstrap | `9c55311` | sys.modules bootstrap |
| Phase 7 | `c055469` | Fresh-clone bootstrap + GitHub readiness |
| **Release** | **`ddd8208`** | **Stabilisierung: Session, Gateway, Audit, CI-ready** |

---

## Ausblick v0.2.0

- Session-Layer vollständig vereinheitlichen
- CLI-Help-Texte von "Hermes" auf "Sidekick" umstellen
- WebUI localStorage-Keys migrieren (`hermes-*` → `sidekick-*`)
- Gateway-Warnings beseitigen
- `cli_backup/` entfernen
- Weitere Smoke-Tests und CI-Erweiterung
